# multi-channel-dispatch-engine

A production-grade dispatch engine for sending messages at scale across multiple channels (WhatsApp, SMS, Email, or any custom provider) with built-in anti-ban protections, concurrency control, and crash recovery.

> Work in progress — refining the public API.
